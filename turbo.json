{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist-cjs/**", "dist-es/**", "dist-types/**", "codegen/**", "*.tsbuildinfo", "out/**"]
    }
  }
}
